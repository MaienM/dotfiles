#!/bin/sh

# Remove a file from the history

git filter-branch \
    --force \
    --index-filter "git rm -r --cached '$file' --ignore-unmatch" \
    --prune-empty \
    --tag-name-filter cat \
    -- --all "$@"
