# Media keys. Fuckers.
"quodlibet --play-pause"
  XF86AudioPlay
"quodlibet --pause"
  XF86AudioStop
"quodlibet --next"
  XF86AudioNext
"quodlibet --previous"
  XF86AudioPrev
"amixer -- sset Master playback $(echo "$(amixer -- sget Master | grep -oE '[-0-9.]+dB' | grep -oE '[-0-9.]+')+2" | bc)dB"
  XF86AudioRaiseVolume
"amixer -- sset Master playback $(echo "$(amixer -- sget Master | grep -oE '[-0-9.]+dB' | grep -oE '[-0-9.]+')-2" | bc)dB"
  XF86AudioLowerVolume
"amixer -- sset Master togglemute"
  XF86AudioMute
