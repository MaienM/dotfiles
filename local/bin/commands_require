#!/usr/bin/env sh

command_require() {
	for command in "$@"; do
		if ! command -v "$command" > /dev/null 2>&1; then
			echo >&2 "Command $command is not available."
			exit 1
		fi
	done
}

if [ "$#" -gt 0 ]; then
	command_require "$@"
fi
