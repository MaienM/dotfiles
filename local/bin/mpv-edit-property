#!/usr/bin/env bash

set -o errexit -o pipefail

file="$(mktemp --suffix='.json')"
mpv-control '{"command":["get_property", "'$1'"]}' | jq '.data' > "$file"
"$EDITOR" "$file"
mpv-control '{"command":["set_property", "vf",'$(cat "$file")']}'
